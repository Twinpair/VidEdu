<% provide(:title, "Your Subjects") %>

<div class="container">
    <div class="row">
      <div class="text-center col-lg-12">
        <h1><b>Your Subject Playlists</b></h1>
        <h4><%= pluralize @subjects.count, "Subjects" %></h4>
      </div>
    </div>
  </div><br>

  <div class="container text-center">
    <div class="row">
      <%= render @subjects %>
    </div>
  </div>