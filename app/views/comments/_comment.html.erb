<li class="media comment" id="comment_<%= comment.id %>">
  <div class="media-body">
    <h4 class="media-heading"><%= User.find(comment.user_id).username %>
      <small class="text-muted">[<%= comment.created_at.to_formatted_s(:long_ordinal) %>]</small></h4>
      <p><%= comment.body %></p>
  </div>
</li>